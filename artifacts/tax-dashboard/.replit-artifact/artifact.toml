kind = "web"
previewPath = "/"
title = "Tax & Accounting Dashboard"
version = "1.0.0"
id = "artifacts/tax-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20561

[services.development]
run = "pnpm --filter @workspace/tax-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tax-dashboard", "run", "build" ]
publicDir = "artifacts/tax-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20561"
BASE_PATH = "/"
